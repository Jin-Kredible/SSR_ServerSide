<?xml version="1.0" encoding="UTF-8"?>
<!-- 
	visitMalls : 방문한 매장 조회
-->
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sinc.ssr.mybatis.malls">
	<select id="visitMalls" parameterType="mallsVO" resultType="mallsVO">
		SELECT  MALL_ID, MALL_NM, MALL_LA, MALL_LONG
		FROM 	MALLS		
	</select>

</mapper>